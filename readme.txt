Hello,
Please make sure to create the env and install these libraries in the terminal before running the simulation thank you:
python -m venv venv
venv/Scripts/activate
pip install simpy pygame matplotlib
